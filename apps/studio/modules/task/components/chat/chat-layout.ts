/** Shared max-width for chat column — aligns messages, tools, and input (Claude-style). */
export const CHAT_COLUMN_CLASS = "mx-auto w-full max-w-3xl px-4 sm:px-6";

/** Task header: title flush-left in main pane (not centered column). */
export const CHAT_HEADER_CLASS = "flex h-12 shrink-0 items-center px-4 sm:px-6";
