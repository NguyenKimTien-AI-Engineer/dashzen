# Task title generation

Given a conversation excerpt, generate a short dashboard task title (3–7 words).

- No quotes
- No punctuation at the end
- If the topic is unclear, respond with exactly: `null`

When `force=true`, you MUST provide a title.
