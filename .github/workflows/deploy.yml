# Deprecated — use `.github/workflows/cd.yml` instead.
# Kept as a manual entry point that forwards to the CD workflow docs.

name: Deploy (legacy)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  redirect:
    name: Use CD workflow
    runs-on: ubuntu-latest
    steps:
      - name: Instructions
        run: |
          echo "Deploy is configured via PaaS Git integration (Option 3)."
          echo ""
          echo "  • CD workflow: .github/workflows/cd.yml"
          echo "  • Guide: infra/deploy/paas.md"
          echo "  • API: Dockerfile + render.yaml"
          echo "  • Studio: apps/studio/vercel.json → Vercel root apps/studio"
          echo ""
          echo "Run the 'CD' workflow manually for optional Render hook + smoke tests."
